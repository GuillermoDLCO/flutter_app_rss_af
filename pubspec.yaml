name: flutter_app_rss_af
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  http: ^0.12.0+2
  webfeed: ^0.4.2
  webview_flutter: ^0.3.10+3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  
  uses-material-design: true
